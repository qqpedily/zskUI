define(function (require) {
    var app = require('../../javascripts/app');

    app.controller('Nav_left_controller', function ($scope, $state, $http) {
        $scope.ngClick = function () {
            $state.go('index.Scholar.scholar_details')
        };
        $rootScope.am();
    });
});
